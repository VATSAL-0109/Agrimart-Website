import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['img.icons8.com', 'res.cloudinary.com', 'static-assets-web.flixcart.com'],
    
  },
};

export default nextConfig;
